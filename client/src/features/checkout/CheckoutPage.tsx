import { Typography } from "@material-ui/core";

export default function CheckoutPage() {
    return (
        <Typography variant='h3'>
            Only logge in users should be able to see this !
        </Typography>
    )
}